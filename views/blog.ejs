<!DOCTYPE html>
<html lang="en" >
<head>
  <meta charset="UTF-8">
  <title>CodePen - vue with blog</title>
  <link rel='stylesheet' href='https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.0/animate.min.css'>
  <link rel="stylesheet" href="stylesheets/blog.css">
  <script src='https://cdnjs.cloudflare.com/ajax/libs/jquery/3.5.1/jquery.min.js'></script>
  <script>
    $(window).on('load',function(){
   // PAGE IS FULLY LOADED  
   // FADE OUT YOUR OVERLAYING DIV
   $('#overlay').fadeOut();
});

  </script>
</head>
<body>
  <div id='overlay' style="  position:fixed;
  z-index:99999;
  top:0;
  left:0;
  bottom:0;
  right:0;
  background:rgba(0,0,0,0.9);
  transition: 1s 0.4s;">
    <h1>Loading...</h1>
  </div>
<!-- partial:index.partial.html -->
<div class="container">
  
  <h1 class="title"><%= blogtitle %></h1>
  <span v-if="cells.length == 0">press <span class="press">Enter</span> on titles if you are done<br>
     to change it just click on the title to change it<br><br>
    press <span class="press">SHIFT+ENTER</span> when you are done writing the textarea<br>
    click on textbox if you wanted to change it<br>
  </span>
  <button v-on:click="cells.push({id:0,title:''})" v-if="cells.length == 0">Start</button>
  <div class='cell' is="cell-item"
      v-for="(cell, index) in cells"
      v-bind:key="cell.id+1"
      v-bind:cellid = 'index+1'
      v-on:remove="cells.splice(cells.map(function(e) { return e.id; }).indexOf(cell.id),1)"
      v-on:add="cells.splice(index+1,0,{id:index+1,title:''});">
  </div>
  
  
</div>


<!-- partial -->
<script src="http://localhost:100/socket.io/socket.io.js"></script>
<script src='https://cdnjs.cloudflare.com/ajax/libs/vue/2.6.11/vue.min.js'></script>

<script  src="javascripts/blog.js"></script>


</body>
</html>